from setuptools import setup, find_packages

setup(
    name='epixerus_database',
    version='0.0.1',
    description='Easy Connect to DB',
    author='WhoisDevelop',
    packages=find_packages(),
    install_requires=[],
)